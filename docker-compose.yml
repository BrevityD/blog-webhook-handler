version: '0.1'

services:
  blog-webhook-handler:
    build: .
    ports:
      - "13134:13134"
    environment:
      - PYTHONUNBUFFERED=1
      - GIT_SSH_COMMAND='ssh -o StrictHostKeyChecking=no'
    volumes:
      - # your volumes
    restart: unless-stopped
